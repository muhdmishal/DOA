<?php

/**
 * Implements hook_preprocess_links().
 */
function doa_preprocess_links(&$variables) {
    if (isset($variables['links']['node-readmore'])) {
      $variables['links']['node-readmore']['link']['#title'] = t('View Details');
    }
}
